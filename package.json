{
  "name": "hayase-rutracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/anilibria.js --bundle --format=esm --outfile=dist/anilibria.js && esbuild src/subtitles.js --bundle --format=esm --outfile=dist/subtitles.js",
    "test": "npm run build && node test/local-test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
